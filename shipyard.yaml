apiVersion: spec.keptn.sh/0.2.0
kind: Shipyard
metadata:
  name: "release-demo"
spec:
  stages:
  - name: staging
    sequences:
    - name: release
      tasks:
      - name: evaluation
        properties:
          timeframe: "5m"
